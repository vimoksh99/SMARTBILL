// Kept empty since all routes were mapped directly in server.js, 
// but to respect the structure:
// In server.js we can theoretically do app.use('/api', require('./routes'))
// Given I already registered them in server.js, no need to overcomplicate.
